{
  "name": "stock-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Product stock tracker with Telegram notifications",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "npm --prefix server install && npm --prefix client install && npm --prefix client run build && rm -rf server/public/* && cp -r client/build/* server/public/",
    "start": "node server/index.js"
  }
}